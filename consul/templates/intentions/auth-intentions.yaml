apiVersion: consul.hashicorp.com/v1alpha1
kind: ServiceIntentions
metadata:
  name: auth-service-intentions
  namespace: app
spec:
  destination:
    name: auth-service
  sources:
    - name: frontend
      action: allow
    - name: "*"
      action: deny
