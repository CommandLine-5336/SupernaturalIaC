apiVersion: consul.hashicorp.com/v1alpha1
kind: ServiceIntentions
metadata:
  name: auth-intentions
  namespace: app
spec:
  destination:
    name: auth
  sources:
    - name: frontend
      action: allow
    - name: "*"
      action: deny
